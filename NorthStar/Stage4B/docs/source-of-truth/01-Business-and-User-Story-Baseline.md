# Business and User Story Baseline — 0.9.0

Maya Chen receives the completed unapproved impact package and the system queues `TOOL-006`. Daniel Brooks can now approve or reject the package through an explicit review contract. A pending review survives process restart; expiry routes to escalation rather than silent acceptance. Aisha Rahman remains a future additional approver for higher-risk dual-control rules.

Stage 4B acceptance: Maya sees a waiting state and deadline; Daniel's decision is validated and single-use; approved, rejected and expired outcomes are distinguishable; no completed tool work is repeated on resume.
