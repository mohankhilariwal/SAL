# 07 - Repository Manifest - S08B Overlay 1.10.0

The Stage 8B overlay adds judge configuration, `JDS-001/1.0.0`, `DATA-143`-`154` schemas, judge modules, evaluation-gate module and script, tests, ADRs, diagrams, reports and references. Runtime code uses the Python standard library. Pytest is the only test dependency. The executable local path uses `PYTHONPATH=src`.

No inherited path is retired. The overlay is compatible with the Stage 8A repository after merge.
