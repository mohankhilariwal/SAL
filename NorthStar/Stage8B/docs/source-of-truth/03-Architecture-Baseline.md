# 03 - Architecture Baseline - S08B Overlay 1.10.0

**Graph:** `GRAPH-001/1.6.0`.

`CMP-008` now contains an advisory judge policy registry, envelope builder, provider-neutral adapter contract, structured-output validator, bias laboratory, calibration analyzer and qualified panel aggregator. The adapter has no approved live route in Stage 8B. `CMP-006` owns human calibration labels and adjudication. `CMP-007` authorizes dataset and rubric access. `CMP-009` records payload-minimized digests.

No new top-level component or active agent is created. `AGT-001` remains exactly one active agent. `WP-008` remains `inactive_future`. `DATA-106` cannot be mutated by evaluation. Model selection/routing remains unresolved. `authority_effect: none` applies to every judge artefact.
