# 00 - Project Constitution - S08B Overlay 1.10.0

All prior NorthStar principles and identifiers remain. Stage 8B introduces model-based judging only as an advisory evaluator inside `CMP-008`.

Constitutional invariants:
- exactly one active `AGT-001`;
- `CMP-003` remains sole workflow/protected-state owner;
- `CMP-007` remains sole authority issuer;
- humans remain approval/finalization owners;
- judge and panel outputs have `authority_effect: none`;
- no hidden chain-of-thought is requested or stored;
- `WP-008` remains `inactive_future`;
- model selection/routing remains unresolved;
- sealed Stage 8A test material is not used in judge calibration.
