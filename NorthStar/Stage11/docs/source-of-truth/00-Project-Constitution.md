# 00 — Project Constitution

**Version:** `1.18.0`  
**Final stage:** `S11`

NorthStar remains a regulated-enterprise tutorial architecture for AI-assisted regulatory impact assessment. The final architecture preserves human accountability, least authority, deterministic enforcement outside the model, evidence provenance, stable identifiers and one cumulative narrative.

## Final constitutional invariants

1. Exactly one active `AGT-001/1.1.0` operates within `GRAPH-001/1.12.0`.
2. `CMP-003` owns routes, protected state, admission, cancellation, aggregation, termination and recovery.
3. `CMP-005` is the only tool, reconciliation and compensation gateway.
4. `CMP-007` is the only authority issuer; `CMP-006` and humans own approval/finalization.
5. One concurrent protected write is the maximum.
6. Audit, authorization, policy, security, integrity, approval, outcome capture and reconciliation cannot be suppressed by cost or error-budget policy.
7. `CAPSTONE-001`, `PRR-001` and all new Stage 11 objects have `authority_effect: none`.
8. Stage 8D and Stage 9D remain unresolved; production is denied.
9. `WP-008`, MCP/A2A and additional agents remain `inactive_future`.
10. The final assignment is educational; no certification or legal conclusion is claimed.
11. Stage 11 ends the tutorial. Unresolved work becomes an implementation backlog, not a silently accepted capability.
