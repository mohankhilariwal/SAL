# 02 — Requirements Register, Stage 10C Overlay

Version `1.17.0`. Preserve all prior requirements.

## Functional

`S10C-FR-001` workload profile; `002` capacity envelope; `003` SLI/SLO proposals; `004` error-budget and control gate; `005` full lifecycle cost events; `006` unit economics; `007` allocation; `008` budgets; `009` forecasts; `010` regional economics; `011` retention/observability/evaluation/human cost; `012` BIA and RTO/RPO; `013` readiness evidence; `014` readiness evaluator; `015` production denial; `016` inherited reliability/security invariants; `017` runnable reference; `018` provider-neutral assumptions.

## Non-functional

`S10C-NFR-001` no authority effect; `S10C-NFR-002` decimal/currency; `S10C-NFR-003` rate assumptions labelled; `S10C-NFR-004` capacity assumptions visible; `S10C-NFR-005` control work cannot be suppressed; `S10C-NFR-006` minimized attribution; `S10C-NFR-007` readiness cannot deploy; `S10C-NFR-008` recovery proposals need approval and exercise; `S10C-NFR-009` regional cost cannot override constraints; `S10C-NFR-010` unresolved hard blockers remain explicit.

Traceability is in Stage 10C Section 22.
