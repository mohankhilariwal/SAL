# ADR 0002: Deterministic policy authority

**Status:** Accepted

The LLM may interpret and explain, deterministic code calculates metrics, and the policy decision point owns authorization. External residual risk requires two independent humans. The export gateway accepts only `ALLOW`. Prompt instructions never become authority.
